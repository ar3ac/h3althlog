{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run (uv) – module",
            "type": "python",
            "request": "launch",
            "module": "${workspaceFolderBasename}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}